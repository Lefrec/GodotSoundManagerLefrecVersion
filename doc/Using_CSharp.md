# Using C#

In order to use this Sound Manager with C# you just need to add a `using Lefrec;` statement to the file and then call PascalCase versions of the functions described in the doc.

```cs
using Lefrec;

SoundManager.playSoundEffect(boom);
```
